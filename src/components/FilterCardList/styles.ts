import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    height: 45,
    margin: 0,
    borderWidth: 1,
    padding: 12,
    borderRadius: 8,
    backgroundColor: '#fff',
    color: 'black',
    borderColor: 'lightgray',
    fontSize: 20,


  },
});
